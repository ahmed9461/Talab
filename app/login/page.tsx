"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000/api/v1";
export default function Login(){const router=useRouter();const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const f=new FormData(e.currentTarget);const r=await fetch(`${API}/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:f.get("username"),password:f.get("password")})});const b=await r.json();setLoading(false);if(!r.ok){setError(b.detail??"تعذر تسجيل الدخول");return}localStorage.setItem("talab_token",b.access_token);router.push("/dashboard")};return <main className="simple-page"><section className="auth-card"><span className="status-pill">تسجيل الدخول</span><h1>مرحبًا بعودتك</h1><p>ادخل بيانات حسابك لمتابعة طلباتك وإشعاراتك.</p><form className="signup-form" onSubmit={submit}><label className="field"><span className="field-label">اسم المستخدم</span><div className="input-shell"><input name="username" autoComplete="username" required/></div></label><label className="field"><span className="field-label">كلمة المرور</span><div className="input-shell"><input name="password" type="password" autoComplete="current-password" required/></div></label>{error&&<p role="alert">{error}</p>}<button className="primary-button" disabled={loading}>{loading?"جارٍ الدخول...":"دخول"}</button></form><p className="login-link">ليس لديك حساب؟ <a href="/">إنشاء حساب</a></p></section></main>}
